/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package space.duel.app;

import space.duel.list.LinkedList;

import static space.duel.utilities.StringUtils.join;
import static space.duel.utilities.StringUtils.split;
import static space.duel.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
